package logica;

public class Main {

    public static void main(String[] args) {
        gui.Conexion conexion = new gui.Conexion();
        conexion.setLocationRelativeTo(null);
        conexion.setVisible(true);
    }
}
